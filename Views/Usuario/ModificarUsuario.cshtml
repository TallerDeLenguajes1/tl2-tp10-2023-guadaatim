@using Kanban.ViewModels

@{
    ViewBag.Title = "Modificar Usuario";
}

@model ModificarUsuarioViewModel

<div class="container">
    <form asp-controller="Usuario" asp-action="ModificarUsuario" class="row">
            <input asp-for="@Model.Id" value="@Model.Id" hidden>
        <div class="col-6">
            <label asp-for="@Model.NombreDeUsuario" class="form-label">Nombre de usuario</label>
                <input asp-for="@Model.NombreDeUsuario" value="@Model.NombreDeUsuario" class="form-control">
        </div>
        <div class="col-6">
            <label asp-for="@Model.Rol" class="form-label">Rol</label>
                <select asp-for="@Model.Rol" class="form-select">
                    <option value="0">Administrador</option>
                    <option value="1">Operador</option>
                </select>
        </div>
        <div class="col-6">
            <label asp-for="@Model.Contrasenia" class="form-label">Contraseña</label>
                <input asp-for="@Model.Contrasenia" value="@Model.Contrasenia" class="form-control">
        </div>
        <div class="col-6">
            <button type="submit" class="btn btn-primary btn-lg">Modificar Usuario</button>
        </div>
        <div class="col-6">
            <button type="button" class="btn btn-primary btn-lg"><a asp-controller="Usuario" asp-action="ListarUsuarios">Cancelar</a></button>
        </div>
    </form>
</div>

@section Scripts {<partial name="_ValidationScriptsPartial"/>}